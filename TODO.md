# TODO

Running list of things we may want to add or change in the future.

## Content
-

## Features
-

## Done
- **Theme switcher** — `theme` command + sidebar dot swatches, 6 themes
  (default, hacker, amber, solarized, paper, grey). Persists in localStorage.
  More themes are ~8 lines of CSS each in `style.css` + one entry in the
  `THEMES` array (terminal.js) + a dot in `index.html`.

## Ideas / maybe
-
